import 'package:flutter/cupertino.dart';

class NetworkDataController with ChangeNotifier {
  // Future<> buyMtnData(DataModel dataModel) async {
  //   var responseData = await ServerRequest()
  //       .postData(path: AppRoute.mtnData, body: dataModel.toJson());
  //   return RegistrationResponse.fromJson(responseData.data);
  // }
}
